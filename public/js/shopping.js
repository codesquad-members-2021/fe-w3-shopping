const pre = document.querySelector('.pre')
const next = document.querySelector('.next')

const topMileageSlide = document.querySelector('.top-mileage-slide')

pre.addEventListener("click", preClickHander)

function preClickHander() {
    topMileageSlide
}